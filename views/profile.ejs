<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/dashboard.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900&display=swap" rel="stylesheet" />
</head>
<body>
    <div class="container">
        <aside>
            <%- include("partial/nav-barAdmin.ejs") %>
        </aside>
        <section class="main">
            <h2>My Profile</h2>
            <div class="profile-container">
                <div class="profile-header">
                    <div class="profile-image">
                        <img src="<%= admin.image || 'https://via.placeholder.com/150' %>" alt="Profile Picture">
                    </div>
                    <div class="profile-info">
                        <h2><%= admin.name %></h2>
                        <p><%= admin.userType %></p>
                        <p><%= admin.address %></p>
                    </div>
                    <div class="profile-edit">
                        <button class="edit-button">Edit <i class="fas fa-edit"></i></button>
                    </div>
                </div>
                <div class="profile-section">
                    <h3>Personal Information</h3>
                    <button class="edit-button">Edit <i class="fas fa-edit"></i></button>
                    <div class="profile-details">
                        <p><strong>Name:</strong> <%= admin.name %></p>
                        <p><strong>Email:</strong> <%= admin.email %></p>
                        <p><strong>Phone:</strong> <%= admin.phoneNumber %></p>
                        <p><strong>Bio:</strong> <%= admin.userType %></p>
                    </div>
                </div>
                <div class="profile-section">
                    <h3>Address</h3>
                    <button class="edit-button">Edit <i class="fas fa-edit"></i></button>
                    <div class="profile-details">
                        <p><strong>Country:</strong> <%= admin.country || 'Not specified' %></p>
                        <p><strong>City/State:</strong> <%= admin.city || 'Not specified' %></p>
                        <p><strong>Postal Code:</strong> <%= admin.postalCode || 'Not specified' %></p>
                        <p><strong>TAX ID:</strong> <%= admin.taxId || 'Not specified' %></p>
                    </div>
                </div>
            </div>
        </section>
    </div>
    <script src="/scripts/dashboard.js"></script>
</body>
</html>
